{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "PersonalQuant": "Debug"
    }
  },

  "// NOTE": "Defaults for running the API directly on the host machine (dotnet run) against the Compose-managed PostgreSQL and Redis. The password is NOT defaulted here — export Postgres__Password or use dotnet user-secrets.",

  "Postgres": {
    "Host": "localhost",
    "Database": "personal_quant",
    "Username": "quant_user",
    "ApplyMigrationsOnStartup": true,
    "SeedReferenceDataOnStartup": true
  },

  "Redis": {
    "Host": "localhost"
  },

  "// MARKET DATA": "No source is registered until FileProviderDirectory names one. Point it at a directory of CSV exports laid out as <interval>/<TICKER>.csv - see data/schemas/market-data-csv.md. A synthetic fixture lives in data/fixtures/market-data.",

  "MarketData": {
    "FileProviderDirectory": "",
    "InstrumentListPath": ""
  },

  "Cors": {
    "AllowedOrigins": "http://localhost:3000,http://localhost:5173"
  }
}
